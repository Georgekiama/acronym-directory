import React from 'react'
import ReactDOM from 'react-dom/client'
import AcronymDirectory from './acronym-directory.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AcronymDirectory />
  </React.StrictMode>,
)
